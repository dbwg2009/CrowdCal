node_modules/
worker/node_modules/
.wrangler/
dist/
worker/dist/
*.log

# Environment and secrets
.env
.env.local
.dev.vars

# Allow example/template files
!.env.example
!.dev.vars.example

# config.local.js is generated at build time from ADMIN_PASSWORD env var
# It's safe to deploy since it's regenerated fresh each time
